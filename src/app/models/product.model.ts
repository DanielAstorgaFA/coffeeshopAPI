export interface IProduct { //Cambiado los campos de producto por el café:
    id:          number;
    title:       string;
    ingredients: string[]
    description: string;
    image:       string;
}
